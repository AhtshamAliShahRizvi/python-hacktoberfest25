def is_armstrong(n):
    num_str = str(n)
    num_digits = len(num_str)
    armstrong_sum = sum(int(d) ** num_digits for d in num_str)
    return armstrong_sum == n

# Test
print(is_armstrong(153))  # True (1^3 + 5^3 + 3^3 = 153)
